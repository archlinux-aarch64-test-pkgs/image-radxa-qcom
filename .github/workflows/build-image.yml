name: Build Radxa Qualcomm KDE Image

on:
  workflow_dispatch:

permissions:
  contents: write

env:
  BUILDER_REPOSITORY: archlinux-aarch64-test-pkgs/arch-image-builder
  BUILDER_REF: custom
  NORMAL_PRESET: arch-ports-radxa-qcom-kde
  UFS_PRESET: arch-ports-radxa-qcom-kde-ufs
  IMAGE_GLOB: ArchLinux-Ports-Radxa-Qualcomm-KDE-*.7z

jobs:
  build-and-release:
    name: Build and publish image
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    container:
      image: ghcr.io/archlinux-aarch64-test-pkgs/build-env:latest
      options: --privileged
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout image repository
        uses: actions/checkout@v4
        with:
          path: image-radxa-qcom

      - name: Checkout arch-image-builder
        uses: actions/checkout@v4
        with:
          repository: ${{ env.BUILDER_REPOSITORY }}
          ref: ${{ env.BUILDER_REF }}
          path: arch-image-builder

      - name: Install image builder dependencies
        run: |
          set -euo pipefail

          pacman -Syu --noconfirm --needed \
            github-cli \
            p7zip \
            rsync \
            wget \
            pyalpm \
            python-yaml \
            python-libarchive-c \
            libisoburn \
            squashfs-tools \
            erofs-utils \
            e2fsprogs \
            btrfs-progs \
            dosfstools \
            python-pip \
            python-setuptools \
            grub \
            sudo \
            jq

          pacman -Scc --noconfirm || true

      - name: Build images
        working-directory: arch-image-builder
        run: |
          set -euo pipefail
          sudo mkdir -p /build/standard /build/ufs
          sudo python build.py -C -p "${NORMAL_PRESET}" -o /build/standard -a artifacts/standard
          sudo python build.py -C -p "${UFS_PRESET}" -o /build/ufs -a artifacts/ufs

      - name: Prepare release assets
        id: assets
        run: |
          set -euo pipefail

          mapfile -t normal_images < <(find arch-image-builder/artifacts/standard -maxdepth 1 -type f -name "${IMAGE_GLOB}" | sort)
          if (( ${#normal_images[@]} != 1 )); then
            echo "::error::Expected exactly one standard image, found ${#normal_images[@]}"
            printf '%s\n' "${normal_images[@]}"
            exit 1
          fi

          mapfile -t ufs_images < <(find arch-image-builder/artifacts/ufs -maxdepth 1 -type f -name "${IMAGE_GLOB}" | sort)
          if (( ${#ufs_images[@]} != 1 )); then
            echo "::error::Expected exactly one UFS image, found ${#ufs_images[@]}"
            printf '%s\n' "${ufs_images[@]}"
            exit 1
          fi

          normal_source="${normal_images[0]}"
          ufs_source="${ufs_images[0]}"
          normal_source_name="$(basename "${normal_source}")"
          timestamp="${normal_source_name#ArchLinux-Ports-Radxa-Qualcomm-KDE-}"
          timestamp="${timestamp%.7z}"

          if [[ ! "${timestamp}" =~ ^[0-9]{14}$ ]]; then
            echo "::error::Could not parse a 14-digit timestamp from ${normal_source_name}"
            exit 1
          fi

          rootfs="/build/standard/radxa-qcom/rootfs"
          if [[ ! -d "${rootfs}/var/lib/pacman/local" ]]; then
            echo "::error::Pacman local database not found under ${rootfs}"
            exit 1
          fi

          mkdir -p release-work/assets
          normal_image="release-work/assets/ArchLinux-Ports-Radxa-Qualcomm-KDE-${timestamp}.7z"
          ufs_image="release-work/assets/ArchLinux-Ports-Radxa-Qualcomm-KDE-UFS-${timestamp}.7z"
          pkglist="release-work/assets/ArchLinux-Ports-Radxa-Qualcomm-KDE-${timestamp}.pkglist.txt"

          install -m 0644 "${normal_source}" "${normal_image}"
          install -m 0644 "${ufs_source}" "${ufs_image}"
          pacman -Q --root "${rootfs}" | LC_ALL=C sort > "${pkglist}"

          {
            echo "normal_image=${normal_image}"
            echo "normal_image_name=$(basename "${normal_image}")"
            echo "ufs_image=${ufs_image}"
            echo "ufs_image_name=$(basename "${ufs_image}")"
            echo "pkglist=${pkglist}"
            echo "pkglist_name=$(basename "${pkglist}")"
            echo "timestamp=${timestamp}"
            echo "tag=v${timestamp}"
            echo "release_title=Arch Linux Ports Radxa Qualcomm KDE ${timestamp}"
          } >> "${GITHUB_OUTPUT}"

      - name: Prepare release notes
        id: notes
        env:
          GH_TOKEN: ${{ github.token }}
          CURRENT_PKGLIST: ${{ steps.assets.outputs.pkglist }}
          NORMAL_IMAGE_NAME: ${{ steps.assets.outputs.normal_image_name }}
          UFS_IMAGE_NAME: ${{ steps.assets.outputs.ufs_image_name }}
          PKGLIST_NAME: ${{ steps.assets.outputs.pkglist_name }}
          NORMAL_PRESET: ${{ env.NORMAL_PRESET }}
          UFS_PRESET: ${{ env.UFS_PRESET }}
        run: |
          set -euo pipefail

          mkdir -p release-work/previous
          previous_tag="$(
            gh release list \
              --repo "${GITHUB_REPOSITORY}" \
              --exclude-drafts \
              --limit 1 \
              --json tagName \
              --jq '.[0].tagName // ""'
          )"
          previous_pkglist=""

          if [[ -n "${previous_tag}" ]]; then
            if gh release download "${previous_tag}" \
                --repo "${GITHUB_REPOSITORY}" \
                --pattern '*.pkglist.txt' \
                --dir release-work/previous \
                --clobber; then
              mapfile -t previous_lists < <(find release-work/previous -maxdepth 1 -type f -name '*.pkglist.txt' | sort)
              if (( ${#previous_lists[@]} == 1 )); then
                previous_pkglist="${previous_lists[0]}"
              else
                echo "::warning::Expected one previous pkglist asset in ${previous_tag}, found ${#previous_lists[@]}"
              fi
            else
              echo "::warning::Could not download previous pkglist asset from ${previous_tag}"
            fi
          fi

          export PREVIOUS_TAG="${previous_tag}"
          export PREVIOUS_PKGLIST="${previous_pkglist}"
          export RELEASE_NOTES="release-work/release-notes.md"

          python - <<'PY'
          from pathlib import Path
          import os

          current_pkglist = Path(os.environ["CURRENT_PKGLIST"])
          previous_pkglist = Path(os.environ["PREVIOUS_PKGLIST"]) if os.environ.get("PREVIOUS_PKGLIST") else None
          previous_tag = os.environ.get("PREVIOUS_TAG", "")
          release_notes = Path(os.environ["RELEASE_NOTES"])
          normal_image_name = os.environ["NORMAL_IMAGE_NAME"]
          ufs_image_name = os.environ["UFS_IMAGE_NAME"]
          pkglist_name = os.environ["PKGLIST_NAME"]
          normal_preset = os.environ["NORMAL_PRESET"]
          ufs_preset = os.environ["UFS_PRESET"]

          def load_pkglist(path: Path) -> dict[str, str]:
              packages: dict[str, str] = {}
              for raw_line in path.read_text(encoding="utf-8").splitlines():
                  line = raw_line.strip()
                  if not line:
                      continue
                  name, version = line.split(maxsplit=1)
                  packages[name] = version
              return packages

          lines = [
              "# Arch Linux Ports Radxa Qualcomm KDE Image",
              "",
              "**Testing status:** untested",
              "",
              "## Important Notices",
              "",
              "- Packages in this image come from Arch Linux Ports, not Arch Linux ARM.",
              "",
              "## Build",
              "",
              f"- Standard preset: `{normal_preset}`",
              f"- UFS preset: `{ufs_preset}`",
              f"- Standard image asset: `{normal_image_name}`",
              f"- UFS image asset: `{ufs_image_name}`",
              f"- Package list asset: `{pkglist_name}`",
              "",
              "## Package Changes",
              "",
          ]

          if previous_pkglist is None:
              if previous_tag:
                  lines.append(
                      f"Package changes compared with `{previous_tag}` are unavailable because no usable previous pkglist asset was found."
                  )
              else:
                  lines.append(
                      "Package changes are unavailable because there is no previous non-draft release with a pkglist asset."
                  )
          else:
              current = load_pkglist(current_pkglist)
              previous = load_pkglist(previous_pkglist)
              added = sorted(set(current) - set(previous))
              removed = sorted(set(previous) - set(current))
              changed = sorted(name for name in set(current) & set(previous) if current[name] != previous[name])

              lines.append(f"Compared with `{previous_tag}`.")
              lines.append("")

              def write_section(title: str, rows: list[str], formatter) -> None:
                  lines.append(f"### {title} ({len(rows)})")
                  lines.append("")
                  if rows:
                      for row in rows:
                          lines.append(formatter(row))
                  else:
                      lines.append("None.")
                  lines.append("")

              write_section(
                  "Added packages",
                  added,
                  lambda name: f"- `{name}` `{current[name]}`",
              )
              write_section(
                  "Removed packages",
                  removed,
                  lambda name: f"- `{name}` `{previous[name]}`",
              )
              write_section(
                  "Updated packages",
                  changed,
                  lambda name: f"- `{name}` `{previous[name]}` -> `{current[name]}`",
              )

          release_notes.write_text("\n".join(lines).rstrip() + "\n", encoding="utf-8")
          PY

          echo "path=${RELEASE_NOTES}" >> "${GITHUB_OUTPUT}"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          gh release create "${{ steps.assets.outputs.tag }}" \
            "${{ steps.assets.outputs.normal_image }}" \
            "${{ steps.assets.outputs.ufs_image }}" \
            "${{ steps.assets.outputs.pkglist }}" \
            --repo "${GITHUB_REPOSITORY}" \
            --target "${GITHUB_SHA}" \
            --title "${{ steps.assets.outputs.release_title }}" \
            --notes-file "${{ steps.notes.outputs.path }}" \
            --prerelease \
            --latest=false
